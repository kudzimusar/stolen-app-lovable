#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running STOLEN Platform Coherence Analysis..."

# Run AI-powered coherence analysis on staged files
npm run coherence:ai-file -- $(git diff --cached --name-only --diff-filter=ACMR | grep -E '\.(ts|tsx)$' | head -1)

# Check if coherence analysis passed
if [ $? -ne 0 ]; then
  echo "❌ Coherence check failed. Commit blocked."
  echo "💡 Review the analysis above and fix any critical issues before committing."
  exit 1
fi

echo "✅ Coherence check passed. Proceeding with commit."